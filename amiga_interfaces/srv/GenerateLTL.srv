# Request
# Natural-language mission, e.g. "visit trees 1 to 3 and sample the leaves at each"
string mission
---
# Response
bool ok
# Promela/SPIN LTL formula; empty when ok is false
string formula
# Failure reason; empty when ok is true
string error
# The model that produced the formula
string model
