Failure reason: {{ reason }}
Objective tree(s) this would drop: {{ dropped_trees }}
Is this failure permanent (the objective can never be completed) or transient (it might still succeed on a later attempt)? Answer with a single word: PERMANENT or TRANSIENT.
